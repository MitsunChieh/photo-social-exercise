<%= simple_format image_tag @photo.img.url %>

Uploader: <%= @photo.user.name %>

<%= simple_format link_to 'Back to index', photos_path %>
<%= simple_format link_to 'Delete tihs photo', photo_path(@photo), method: :delete %>