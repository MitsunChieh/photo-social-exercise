<% if @photo.empty? %>
  Do not have liked photo<br>
<% else %>
  <% @photo.each do |photo|%>
    <%= link_to image_tag( photo.img.url(:medium) ), photo_path(photo) %>
  <% end %>
<% end %>

<br>
<%= link_to 'Back to user\'s own photo', user_path(@user) %>