<%= simple_format @user.name %>

<% @user.photos.each do |photo|%>
  <%= link_to image_tag( photo.img.url(:medium) ), photo_path(photo) %>
<% end %>

<!-- <%= simple_format link_to 'Back to user index', users_path %> -->
<%= simple_format link_to 'Go to photo index', photos_path %>